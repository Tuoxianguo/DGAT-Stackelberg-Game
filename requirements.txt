# Core runtime — enough to train DGAT++, run ensembles, Stackelberg optimization and plots.
# Reference environment: Python 3.10, CUDA 12.1/12.2, single Tesla T4.

numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
h5py>=3.9
pyarrow>=14
pyyaml
tqdm
matplotlib>=3.7
seaborn>=0.12

# Deep learning backbone
torch>=2.2          # install the CUDA build matching your driver, e.g.
                    #   pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121

# ---------------------------------------------------------------------------
# Optional — only needed for specific baselines / utilities (uncomment as needed)
# ---------------------------------------------------------------------------
# transformers>=4.42        # BatteryGPT-Lite (causal-Transformer) baseline only
# torch_geometric>=2.5      # the proposed HGNN is hand-rolled; PyG only for ablations
# networkx>=3.1             # graph utilities / some plots
# hmmlearn>=0.3             # reference HMM comparison (HSMM itself is custom & differentiable)
# xgboost                   # optional gradient-boosting baseline
# lightgbm                  # optional gradient-boosting baseline
# huggingface_hub>=0.24     # dataset download helpers
# datasets>=2.20            # dataset download helpers
# python-docx>=1.2          # tools/: markdown -> docx
# pypandoc-binary>=1.17     # tools/: markdown -> docx (bundles pandoc)
